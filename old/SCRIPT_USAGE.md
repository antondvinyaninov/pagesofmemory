# Скрипт управления проектом Memory

Универсальный скрипт `run` для управления проектом Memory.

## Быстрый старт

```bash
# Запуск проекта
./run

# Или явно
./run start
```

## Команды

### `./run start` (по умолчанию)
Запускает весь проект:
1. 🧹 Очищает порты 3000 и 5000
2. 📦 Проверяет и устанавливает зависимости
3. ⚙️ Настраивает переменные окружения
4. 🔨 Собирает backend
5. 🚀 Запускает backend на порту 5000
6. 🌐 Запускает frontend на порту 3000
7. ✅ Проверяет готовность сервисов

### `./run stop`
Останавливает все процессы:
- Завершает процессы backend и frontend
- Очищает порты 3000 и 5000
- Удаляет PID файлы

### `./run restart`
Перезапускает проект:
- Останавливает все процессы
- Ждет 2 секунды
- Запускает проект заново

### `./run status`
Проверяет статус серверов:
- Проверяет занятость портов
- Тестирует доступность API
- Показывает текущее состояние

### `./run install`
Устанавливает зависимости:
- Устанавливает npm пакеты для backend
- Устанавливает npm пакеты для frontend
- Настраивает переменные окружения

## Логи

Скрипт создает файлы логов в корне проекта:
- `backend.log` - логи backend сервера
- `frontend.log` - логи frontend сервера

Для просмотра логов в реальном времени:
```bash
# Backend логи
tail -f backend.log

# Frontend логи
tail -f frontend.log
```

## Автоматические проверки

Скрипт автоматически:
- ✅ Проверяет наличие Node.js и npm
- ✅ Проверяет установленные зависимости
- ✅ Создает .env файлы из config.env
- ✅ Ждет полного запуска сервисов
- ✅ Проверяет доступность API
- ✅ Сохраняет PID процессов для корректной остановки

## Устранение проблем

### Порты заняты
```bash
# Принудительная очистка портов
./run stop
```

### Проблемы с зависимостями
```bash
# Переустановка зависимостей
./run install
```

### Проблемы с сборкой
```bash
# Проверка логов
cat backend.log
cat frontend.log
```

### Полный сброс
```bash
./run stop
rm -f backend.log frontend.log
rm -rf backend/node_modules frontend/node_modules
./run install
./run start
```

## Переменные окружения

Скрипт автоматически копирует:
- `backend/config.env` → `backend/.env`
- `frontend/config.env` → `frontend/.env.local`

## Требования системы

- Linux/macOS
- Node.js 18+
- npm
- lsof (для проверки портов)
- curl (для проверки доступности)

## Примеры использования

```bash
# Обычная разработка
./run                    # Запуск
# ... работа ...
./run stop              # Остановка

# Быстрый перезапуск при изменениях
./run restart

# Проверка что все работает
./run status

# Первоначальная настройка проекта
./run install
./run start
```
