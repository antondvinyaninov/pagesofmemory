# Memory Platform

Веб-приложение для управления воспоминаниями и знаниями с системой регистрации и авторизации.

## Технологический стек

### Фронтенд
- **Next.js 15** - React фреймворк
- **TypeScript** - типизация
- **TailwindCSS** - стилизация
- **React Hook Form** - управление формами
- **Zod** - валидация схем
- **Axios** - HTTP клиент
- **Hero Icons** - набор SVG иконок от создателей TailwindCSS

### Бекенд
- **Node.js** - серверная среда
- **Express** - веб-фреймворк
- **TypeScript** - типизация
- **SQLite** - база данных
- **JWT** - аутентификация
- **bcryptjs** - хеширование паролей
- **Zod** - валидация данных
- **Lodash** - утилиты

## Структура проекта

```
memory/
├── backend/              # Серверная часть
│   ├── src/
│   │   ├── controllers/  # Контроллеры
│   │   ├── middleware/   # Middleware
│   │   ├── models/       # Модели и работа с БД
│   │   ├── routes/       # Маршруты API
│   │   ├── types/        # TypeScript типы
│   │   ├── utils/        # Утилиты
│   │   ├── config.ts     # Конфигурация
│   │   └── index.ts      # Точка входа
│   ├── package.json
│   ├── tsconfig.json
│   └── config.env        # Переменные окружения
├── frontend/             # Клиентская часть
│   ├── src/
│   │   ├── app/          # App Router страницы
│   │   ├── components/   # React компоненты
│   │   ├── contexts/     # React контексты
│   │   ├── services/     # API сервисы
│   │   └── types/        # TypeScript типы
│   ├── package.json
│   └── config.env        # Переменные окружения
└── README.md
```

## Установка и запуск

### Предварительные требования
- Node.js (версия 18 или выше)
- npm или yarn
- lsof (для проверки портов)

### 🚀 Быстрый запуск (рекомендуется)

```bash
# Клонирование репозитория
git clone <repository-url>
cd memory

# Запуск проекта одной командой
./run
```

Готово! Проект автоматически:
- Очистит порты 3000 и 5000
- Установит зависимости (если нужно)
- Настроит переменные окружения
- Соберет и запустит backend
- Запустит frontend

### 📋 Управление проектом

```bash
# Запуск проекта
./run start    # или просто ./run

# Остановка проекта
./run stop

# Перезапуск проекта
./run restart

# Проверка статуса
./run status

# Установка зависимостей
./run install

# Справка
./run help
```

### 🌐 URL адреса
- **Frontend:** http://localhost:3000
- **Backend API:** http://localhost:5000
- **Health Check:** http://localhost:5000/health

### 📄 Логи
- Backend: `backend.log`
- Frontend: `frontend.log`

### 🔧 Ручная настройка (если нужно)

<details>
<summary>Развернуть инструкции по ручной настройке</summary>

#### Настройка бекенда

```bash
# Перейти в папку бекенда
cd backend

# Установить зависимости
npm install

# Скопировать файл переменных окружения
cp config.env .env

# Собрать проект
npm run build

# Запустить в режиме разработки
npm run dev
```

#### Настройка фронтенда

```bash
# Перейти в папку фронтенда
cd frontend

# Установить зависимости
npm install

# Скопировать файл переменных окружения
cp config.env .env.local

# Запустить в режиме разработки
npm run dev
```

</details>

## API Endpoints

### Аутентификация

#### POST /api/auth/register
Регистрация нового пользователя

**Request Body:**
```json
{
  "name": "Имя пользователя",
  "email": "user@example.com",
  "password": "password123"
}
```

**Response:**
```json
{
  "message": "Пользователь успешно зарегистрирован",
  "user": {
    "id": 1,
    "name": "Имя пользователя",
    "email": "user@example.com",
    "createdAt": "2025-01-01T00:00:00.000Z",
    "updatedAt": "2025-01-01T00:00:00.000Z"
  },
  "token": "jwt_token_here"
}
```

#### POST /api/auth/login
Авторизация пользователя

**Request Body:**
```json
{
  "email": "user@example.com",
  "password": "password123"
}
```

**Response:**
```json
{
  "message": "Авторизация успешна",
  "user": {
    "id": 1,
    "name": "Имя пользователя",
    "email": "user@example.com",
    "createdAt": "2025-01-01T00:00:00.000Z",
    "updatedAt": "2025-01-01T00:00:00.000Z"
  },
  "token": "jwt_token_here"
}
```

#### GET /api/auth/profile
Получение профиля пользователя (требует авторизации)

**Headers:**
```
Authorization: Bearer jwt_token_here
```

**Response:**
```json
{
  "user": {
    "id": 1,
    "name": "Имя пользователя",
    "email": "user@example.com",
    "createdAt": "2025-01-01T00:00:00.000Z",
    "updatedAt": "2025-01-01T00:00:00.000Z"
  }
}
```

## Особенности

### Безопасность
- Пароли хешируются с помощью bcryptjs
- JWT токены для аутентификации
- CORS настроен для защиты от межсайтовых запросов
- Helmet для базовой безопасности заголовков

### Валидация
- Zod схемы для валидации на фронтенде и бекенде
- React Hook Form для управления формами
- Обработка ошибок API

### UI/UX
- Responsive дизайн с TailwindCSS
- Современный интерфейс с Hero Icons
- Состояния загрузки и обработка ошибок
- Доступность (accessibility) компонентов
- Удобная система работы с иконками

## База данных

Приложение использует SQLite базу данных, которая автоматически создается при первом запуске бекенда. Файл базы данных: `backend/database.sqlite`

### Схема таблицы users:
```sql
CREATE TABLE users (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  email TEXT UNIQUE NOT NULL,
  password TEXT NOT NULL,
  name TEXT NOT NULL,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

## Разработка

### Скрипты бекенда:
- `npm run dev` - запуск в режиме разработки с hot reload
- `npm run build` - сборка TypeScript проекта
- `npm start` - запуск собранного проекта
- `npm test` - запуск тестов

### Скрипты фронтенда:
- `npm run dev` - запуск в режиме разработки
- `npm run build` - сборка production версии
- `npm start` - запуск production версии
- `npm run lint` - проверка кода ESLint

## Проверка работы

1. Запустите бекенд и фронтенд согласно инструкциям выше
2. Откройте http://localhost:3000 в браузере
3. Попробуйте зарегистрировать нового пользователя
4. Войдите с созданными учетными данными
5. Проверьте, что после авторизации отображается персонализированная главная страница

## Health Check

Проверить работу бекенда можно по адресу: http://localhost:5000/health

## Работа с иконками

Проект использует Hero Icons - официальный набор иконок от создателей TailwindCSS.

### Доступные иконки

В файле `src/components/icons/Icons.tsx` экспортированы самые популярные иконки:

```tsx
import { HomeIcon, UserIcon, SearchIcon } from '@/components/icons';
```

### Универсальный компонент Icon

Для удобной работы с иконками создан компонент `Icon`:

```tsx
import { Icon, HomeIcon } from '@/components/icons';

// Базовое использование
<Icon icon={HomeIcon} />

// С настройками размера и цвета
<Icon 
  icon={UserIcon} 
  size="lg" 
  color="text-blue-600" 
  onClick={handleClick}
  aria-label="Профиль пользователя"
/>
```

### Размеры иконок

Доступные размеры:
- `xs` - 12px (h-3 w-3)
- `sm` - 16px (h-4 w-4)  
- `md` - 20px (h-5 w-5) - по умолчанию
- `lg` - 24px (h-6 w-6)
- `xl` - 32px (h-8 w-8)

### Добавление новых иконок

1. Импортируйте нужную иконку из `@heroicons/react/24/outline` или `@heroicons/react/24/solid`
2. Добавьте её в экспорт в файле `src/components/icons/Icons.tsx`
3. Используйте через компонент `Icon` или напрямую
